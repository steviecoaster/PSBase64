# PSBase64 -- _Work with Base64 strings the easy way_

PSBase64 includes a set of cmdlets that makes working with Base64 encoded strings easier. These cmdlets are `ConvertTo-Base64String` and `ConvertFrom-Base64String`.